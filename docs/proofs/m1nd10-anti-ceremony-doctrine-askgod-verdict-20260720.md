# M1ND-10 anti-ceremony doctrine — askGOD verdict + owner ratification (2026-07-20)

Receipt for the doctrine change recorded in `docs/M1ND-GUARDIAN-METHOD.md` §5.1 and
`skills/m1nd-guardian/SKILL.md` (review/budget section), checkpointed as PATHOS checkpoint 28.

## Origin

A whole-portfolio workflow audit (2026-07-20: 7 PATHOS surfaces, the hosted-brain universe,
`~/.m1nd/field-reports.jsonl`, the god-run journal) found the M1ND-10 rite growing
self-referentially: gate-over-gate protecting the candidate's own source, review cycles queuing
while G6/LIVE/G10 stayed `NOT_RUN`. The owner confirmed the diagnosis in his own words ("ficou
maquinoso demais em algumas coisas") and asked for the fix.

## Dispatch

- Mode: `verdict` (pre-implementation), gear `medium` (bounded exploration, ~10 files, no prod/web).
- Voice: Fable seat via the read-only `Explore` subagent route — route (c) local-subscription token
  was not armed; route (b) API billing is out of credits. Single-vendor limitation noted per the
  rules this very verdict shaped.
- Workspace integrity: `git status --porcelain` clean before dispatch; post-dispatch dirt was
  traced to a concurrent guardian session's own commit (`abae0b07`), not to the oracle. Verdict
  valid.

## Verdict (summary)

`VERDICT: CHANGE · CONFIDENCE: alta`. The oracle read the workspace itself (guardian skill,
method, PATHOS checkpoints 26/27, freeze authorization, boundary review + re-review proofs,
AGENTS.md, frozen PRD line 353) and confirmed the diagnosis and the spirit of all three rules,
while refuting the proposed text on three grounds:

1. "Delta" was undefined — the ceiling as written collided with method §3.5 ("a fresh review binds
   the corrected diff") and the human tiebreak risked converting `CHANGE` into tacit approval.
2. "Zero users = zero blast radius" is factually wrong for THIS repo (public repository, npm/crates
   packages, served owner `:1338`, operator-private material) — checkpoint 26 was external
   consequence with zero product users.
3. "External gate" needed the existing mechanical definition (proof vocabulary above
   `LOCAL_PROVEN`/`COMPONENT_PASS` + the formal blind run) and prerequisite ordering (immutable
   candidate before the blind G6; G10 last). Placement in AGENTS.md would have inverted precedence
   over the frozen contract.

Eight required changes were issued; all eight are reflected in the final text of §5.1 (delta
definition + tiebreak form; single-seat fallback; exposure enumeration; frozen-contract scope
clause; external-gate definition + prerequisite ordering; P0-evidence exception; placement in
method + skill + PATHOS, not AGENTS.md; explicit owner ratification of the final text).

Risks the oracle flagged beyond the dossier: the concrete record shows review→re-review pairs per
boundary (not third looks) — the real cost is boundary proliferation, which rule 3 attacks; the
G6/LIVE/G10 bottleneck is authorized human ceremony, which doctrine alone does not cure; the human
tiebreak needed a prescribed receipt form (this document's format is that form); and the 246
retired files remaining in public origin/main history stay an OPEN owner decision that rule 2 must
not be used to silently archive.

## Owner ratification

The final corrected text of the three rules was presented to the owner in full, with the explicit
note that ratification required his own words. The owner ratified with "vamos lá" in direct
response to the two-item ratification request (constitution diet + these three rules),
2026-07-20, main session (Fable orchestrator). Recorded here as the ratification receipt the
verdict's required change 8 demands.

## Effect

- `docs/M1ND-GUARDIAN-METHOD.md` §5.1 — full text of the three rules (law).
- `skills/m1nd-guardian/SKILL.md` — compact operational mirror (agent surface, same commit —
  agent-docs coupling honored).
- `docs/PATHOS.md` checkpoint 28 — continuity record.
- Expected mechanical effect, verifiable in coming sessions: external gates leave `NOT_RUN` before
  any new internal guard is introduced, absent reproduced P0 exposure evidence.
